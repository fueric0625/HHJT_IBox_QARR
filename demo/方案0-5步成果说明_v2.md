# 出差制度智能体 DEMO：第 1–5 步第二轮成果说明

- 对应方案：`files/规章制度智能体召回优化方案v0.2.md` 第 380–386 行（第 0 步不重做）
- 对照冻结：[`方案0-5步成果说明_v1.md`](方案0-5步成果说明_v1.md)
- 评测快照：`eval/eval_report.md`（2026-09-17T16:27:04）
- 本轮仍不上：向量检索、BM25（第 6 步）、跨会话上下文产品化（第 7 步）、部署评估（第 9 步）

## 1. 相对第一轮改了什么

| 步骤 | 改动 |
|---|---|
| 1 提示 + 评测 | 取消强制先 `list_catalog`；要求并行读含表/命中/see_also；无据题必须写「未在制度库中找到」。评测增加耗时、轮次、并行批、是否空转根目录。 |
| 2 目录 | `build_catalog.py --llm` 按章生成条款摘要与关键词；条款 ID 校验保留。文件/章级易混点用 `FILE_SUMMARIES` / `CHAPTER_SUMMARIES` 回写，避免 LLM 冲掉「公派车辆在第十三条、津贴在第十二条」。 |
| 3 工具 | 首轮注入根目录 + 字面检索线索；`list_section` 按问题排序；`read_clause` 返回 `see_also` 与 `coverage_hint`；`KEEP_FULL_CLAUSES=4`。 |
| 4 前端 | SSE 轨迹显示轮次与累计耗时。协议不变。 |
| 5 闭环 | 流水线默认 `--llm`；冒烟断言第十二/十三条关键词、公派车辆导航线索、第八条 see_also 指向第十三条。 |

## 2. 效果对照（自动判定）

| 指标 | 第一轮（11:29） | 第二轮（16:27） |
|---|---|---|
| likely_ok | 8 | 10 |
| unanswerable_ok / fail | 0 / 1 | **1 / 0** |
| partial | 3 | 6 |
| miss_clause | 5 | **0** |
| likely_ok + unanswerable_ok | 8 | **11** |
| 中位轮次（含作答） | 约 5 | **2.0** |
| 仍调用 list_catalog 根节点 | 几乎每题 | **0/17** |
| 使用并行工具调用 | 基本无 | **10/17** |
| 中位耗时 | 未记 | 29.44s |

目标条款：

- tc-04 公派车辆：读到 `3-1-13:第十三条`，`likely_ok`
- tc-17 津贴：读到 `3-1-13:第十二条`，`likely_ok`
- tc-01 无据：`unanswerable_ok`

`miss_clause` 已清零。`likely_ok + unanswerable_ok` 为 11/17，距「≥12」差 1，差额在 **partial**（读到金标准但关键词未齐或跨条未读全），不是导航完全走偏。

## 3. 仍弱的题（天花板能定位）

| ID | 判定 | 说明 |
|---|---|---|
| tc-02 | partial | 已读第八条+第十三条，金标准还有第十六条、报销第十条 |
| tc-06 | partial | 已读第五条与商旅第八条，答案关键词未齐 |
| tc-07 | partial | 读到交通/住宿表，津贴表/附件 1 未齐 |
| tc-08 | partial | 停在第九条，第十七条超标自理未读 |
| tc-11 | partial | 读到出差第七条商旅预定，未读 3-1-21 第四/六条 |
| tc-16 | partial | 已读逾期第二十一、二十二条，答案未写出「3个月」字面 |

这些适合留给第 6 步（BM25/向量与目录并行）或再收紧跨文件组合，而不是再堆个案提示。

## 4. 标准做法（可重跑）

```powershell
cd E:\ibox_qarr\demo
python scripts\run_pipeline.py          # extract → 用例 → catalog --llm → 冒烟
python scripts\build_catalog.py --hints-only   # 只把文件/章易混点写回已有目录
python eval\run_eval.py                 # 17 题，含轮次/耗时
python -m uvicorn backend.app:app --host 127.0.0.1 --port 8000
```

重抽 PDF 或改 `clauses.jsonl` / `catalog.json` 后必须重启 uvicorn。
