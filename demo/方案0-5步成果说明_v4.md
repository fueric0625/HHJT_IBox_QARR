# 出差制度智能体 DEMO：第 1–5 步第四轮成果说明

- 对应方案：`files/规章制度智能体召回优化方案v0.2.md` 第 380–386 行（分级目录 + 渐进式披露）及第 123–126 行（先看目录再下钻）
- 对照冻结：[`方案0-5步成果说明_v3.md`](方案0-5步成果说明_v3.md)
- 本轮仍不上：向量检索、BM25（第 6 步）、跨会话上下文产品化（第 7 步）、部署评估（第 9 步）
- 评测 17 题口径不变；本轮未重跑 `eval/run_eval.py`，自动判定仍以第二轮快照为准

## 1. 相对第三轮改了什么

第三轮已把「最终作答真流式 + 添加 PDF」做成。本轮只改**召回入口**：问题进来后不再直接调工具，先用大模型对照目录判断方向并圈章节。

| 项 | 改动 |
|---|---|
| 路由轮（无工具） | `backend/route.py`。把 `catalog.json` 压成文件+章的标题/摘要/条款号清单（不含原文），交给独立系统提示，只输出 JSON：`direction`、`reason`、`candidates`（2～6 个真实 id）、`maybe_unanswerable`。 |
| 校验 | 候选 id 必须能在目录里找到，幻觉条款号直接丢掉。路由失败或候选为空时，退回原来的根目录注入 + 字面检索。 |
| 注入工具轮 | 路由结果写成「路由判断」块，与字面检索线索一起贴进用户消息。系统提示改为：先 `list_section` 拟引用章节，线索里已有条款 id 则可并行 `read_clause`；禁止按路由硬答。 |
| SSE | 新增 `route_start`、`route`（在第一轮 `round_start` / `tool_call` 之前）。`ask()` 把 `route` 记入 trace。 |
| 前端 | 工具轨迹顶部用蓝色显示「正在判断…」和「方向：…拟引用：…」。 |

抽查（未计入评测表）：

- 「公司出差津贴的计算标准」→ 方向「出差津贴标准」，拟引用 `3-1-13:第五章`，随后 `read_clause` `3-1-13:第十二条`
- 「公派车辆出差，是否还能报销交通费用？」→ 方向「公派车辆能否报销交通费」，拟引用 `3-1-13:第五章`，随后读到 `3-1-13:第十三条`

路由本身约 8 秒（一次非流式补全，不计工具轮次）。

## 2. 标准用法

```powershell
cd E:\ibox_qarr\demo
python -m uvicorn backend.app:app --host 127.0.0.1 --port 8000
```

- 浏览器打开 `http://127.0.0.1:8000`（改过前端后请硬刷新）
- 提问后轨迹顺序：判断方向 → 拟引用章节 → `list_section` / `read_clause` → 【直接回答】真流式
- 聊天 SSE：`route_start` / `route` / `round_start` / `tool_call` / `tool_result` / `answer_start` / `token` / `done` / `error`

评测仍走 `python eval\run_eval.py`（内部 `ask()`，同样会先路由再调工具）。

## 3. 不在本轮

未重跑 17 题，不把抽查当成新的 `likely_ok` 数字。删除文件、Word/md、BM25/向量、跨会话产品化、改 IBox 前端，仍未做。
